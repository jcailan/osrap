@EndUserText.label: 'Access Control for Travel'
@MappingRole: true
define role ZAC_JC_TRAVEL {
    grant
        select
            on
                ZC_JC_TRAVEL
                    where
                        inheriting conditions from entity ZI_JC_Travel;
                        
}